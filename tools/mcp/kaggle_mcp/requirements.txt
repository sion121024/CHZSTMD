# Kaggle MCP server dependencies
mcp>=1.2
kaggle>=1.6

# Note: payload/train_dialogue.py runs inside the Kaggle kernel and uses the
# torch / sentencepiece already installed there. The MCP server host does not
# need them.
