# Core (incl. the from-scratch neural net) runs on the standard library only.
# The model is NOT an external pretrained model: it is defined from scratch in
# src/broadcast_ai/nn + model/unified.py. So there is no LLM runtime dependency.
#
# The packages below are optional, local-only, and used only when you turn on
# real voice / screen capture / numeric acceleration. No external API is ever used.

# --- Low-latency local TTS (pick one) ---
# piper-tts                    # on-device low-latency TTS
# TTS                          # Coqui XTTS family

# --- Screen capture / OCR (in-game tutorial auto-learning) ---
# mss                          # fast screen capture
# pillow                       # image processing
# pytesseract                  # on-device OCR

# --- Numeric acceleration (optional) ---
# numpy>=1.26                  # swap the nn backend to numpy for higher Hz
