# 핵심 런타임은 표준 라이브러리만으로 동작합니다 (오프라인 폴백 포함).
# 아래는 실제 로컬 모델/GPU/음성/화면인식을 켤 때 선택적으로 설치하는 의존성입니다.
# 전부 "로컬" 패키지이며 외부 API 호출은 사용하지 않습니다.

# --- 단일 로컬 LLM (택1) ---
# llama-cpp-python>=0.2.90      # GGUF 양자화 모델, CPU/GPU(CUDA/Metal) 친화적
# transformers>=4.44           # HF 로컬 추론
# torch>=2.3                   # GPU 텐서 백엔드

# --- 저지연 로컬 TTS (택1) ---
# piper-tts                    # 초저지연 온디바이스 TTS
# TTS                          # Coqui XTTS 계열

# --- 화면 인식 / OCR (게임 튜토리얼 자동 학습) ---
# mss                          # 빠른 화면 캡처
# pillow                       # 이미지 처리
# pytesseract                  # 온디바이스 OCR

# --- 수치 가속 (선택) ---
# numpy>=1.26
